# @package _global_
defaults:
  - override /alg: sgd
  - override /agent: n_fashionmnist
  - override /task: fashionmnist


run:
    seed: 0
    num_seeds: 5
    outdir: logs/static/
    log_every: 2000
