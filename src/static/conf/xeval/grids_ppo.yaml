# @package _global_
defaults:
  - /xper: grids_ppo

eval:
  alg: ${load:alg/ppo}
  task: ${load:task/grids}
  agent: ${load:agent/ac_minigrid}
  evaluator: ${load:evaluators/rl}
  total_timesteps: 10 
  seed: 1234
  num_seeds: 1
