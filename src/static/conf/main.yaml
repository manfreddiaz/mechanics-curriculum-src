defaults:
    - _self_
    - alg: ppo
    - agent: ac_mlp
    - task: sipd

run:
    seed: 0
    num_seeds: 5
    outdir: logs/static/
    pool: 2
    async: false
    deterministic: false  
    device: cuda

