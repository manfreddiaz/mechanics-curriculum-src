# @package _global_
defaults:
  - override /alg: ppo
  - override /agent: ac_minatar
  - override /task: minatar

alg:
  rparams:
    total_timesteps: 1000000

run:
    seed: 0
    num_seeds: 5
    outdir: logs/static/